<template>
  <view class="index">
<% if (uiLibrary === '@nutui/nutui') { -%>
    <nut-cell-group :title="'<%= projectName %>'">
      <nut-cell title="框架" desc="Taro + Vue3 + NutUI" />
      <nut-cell title="计数" :desc="String(count)" />
    </nut-cell-group>
    <view class="action">
      <nut-button type="primary" block @click="increment">点击 +1</nut-button>
    </view>
<% } else { -%>
    <text class="title"><%= projectName %></text>
    <text class="desc">Taro + Vue3<% if (uiLibrary === 'vant') { %> + Vant<% } %> 跨端项目</text>
    <text class="count">计数: {{ count }}</text>
    <button @tap="increment">点击 +1</button>
<% } -%>
  </view>
</template>

<script setup lang="ts">
import { ref } from 'vue'
import { useLoad } from '@tarojs/taro'

const count = ref(0)

useLoad(() => {
  console.log('Page loaded.')
})

function increment() {
  count.value += 1
}
</script>

<style lang="scss">
.index {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 48px 24px;
}

<% if (uiLibrary !== '@nutui/nutui') { -%>
.title {
  font-size: 24px;
  font-weight: 700;
  margin-bottom: 8px;
}

.desc {
  color: #666;
  margin-bottom: 16px;
}

.count {
  margin-bottom: 16px;
}
<% } -%>

.action {
  width: 100%;
  margin-top: 24px;
  padding: 0 24px;
  box-sizing: border-box;
}
</style>
