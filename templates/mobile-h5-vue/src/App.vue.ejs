<script setup<% if (useTypeScript) { %> lang="ts"<% } %>>
import { ref } from 'vue'

const count = ref(0)
</script>

<template>
<% if (uiLibrary === '@nutui/nutui') { -%>
  <nut-navbar title="<%= projectName %>" />
  <div class="page">
    <nut-cell-group title="H5 移动端">
      <nut-cell title="框架" desc="Vue 3 + NutUI" />
      <nut-cell title="计数" :desc="String(count)" />
    </nut-cell-group>
    <div class="action">
      <nut-button type="primary" block @click="count++">
        点击 +1
      </nut-button>
    </div>
  </div>
<% } else { -%>
  <van-nav-bar title="<%= projectName %>" fixed placeholder />
  <div class="page">
    <van-cell-group inset title="H5 移动端">
      <van-cell title="框架" value="Vue 3 + Vant" />
      <van-cell title="计数" :value="String(count)" />
    </van-cell-group>
    <div class="action">
      <van-button type="primary" block round @click="count++">
        点击 +1
      </van-button>
    </div>
  </div>
<% } -%>
</template>

<style scoped>
.page {
  padding: 16px;
}

.action {
  margin-top: 24px;
  padding: 0 16px;
}
</style>
