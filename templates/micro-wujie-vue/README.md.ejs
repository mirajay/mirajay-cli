# <%= projectName %>

无界 (wujie) 微前端 — **Vue 3 主应用**模板。

## 说明

- 本仓库只生成 **Vue 主应用**（`wujie-vue3`），不包含 `react` 依赖
- `sub-app-vue` 为示例子应用标识，需在 `http://localhost:5174` 独立启动
- 子应用技术栈不限，可用本脚手架再生成一个 Vue 项目作为 remote

## 开发

```bash
pnpm dev
```
