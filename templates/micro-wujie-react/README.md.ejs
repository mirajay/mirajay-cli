# <%= projectName %>

无界 (wujie) 微前端 — **React 主应用**模板。

## 说明

- 本仓库只生成 **React 主应用**（`wujie-react`），不包含 `vue` 依赖
- `sub-app-react` 为示例子应用标识，需在 `http://localhost:5174` 独立启动
- 子应用技术栈不限，可用本脚手架再生成一个 React 项目作为 remote

## 开发

```bash
pnpm dev
```
