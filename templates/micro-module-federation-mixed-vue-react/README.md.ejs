# <%= projectName %>

Module Federation **混合栈演示** Monorepo（Vue 主应用 + React 远程模块）。

## 架构

- `apps/host` - Vue 主应用，通过桥接组件挂载 React 远程模块
- `packages/remote-app` - React 子应用，暴露 Header 组件

> 混合栈适合演示与存量系统集成；生产环境同栈 Module Federation 更简单、类型与共享依赖更可控。

## 开发

```bash
# 终端 1: 启动 remote（React）
cd packages/remote-app && pnpm dev

# 终端 2: 启动 host（Vue）
cd apps/host && pnpm dev
```

## 构建

```bash
pnpm build
```
